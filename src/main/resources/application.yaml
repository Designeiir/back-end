server:
  port: 10100

mybatis:
  config-location: classpath:mybatis.cfg.xml
  type-aliases-package: com.example.backend.entity
  mapper-locations:
    - classpath:mapper/*.xml


spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/itcast?serverTimezone=GMT%2B8&useSSL=FALSE&allowPublicKeyRetrieval=true
    username: root
    password: 52118259541xx
    type: com.alibaba.druid.pool.DruidDataSource

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  global-config:
    db-config:
      id-type: auto
      field-strategy: IGNORED
      #
      db-type: MYSQL
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
